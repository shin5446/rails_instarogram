<div class = "index_page">
<p id="notice"><%= notice %></p>

<h1>みんなの投稿</h1>

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>内容</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <td><%= blog.content %></td>
        <td class="btn btn-default"><%= link_to '詳細をみる', blog %></td>
        <%if current_user.try(:id) == blog.user_id %>
        <td class="btn btn-default"><%= link_to '編集する', edit_blog_path(blog) %></td>
        <td class="btn btn-default"><%= link_to '削除する', blog, method: :delete, data: { confirm: '消してもええんか？' } %></td>
　　　　<% end %>      
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p class="btn btn-default"><%= link_to '新規投稿', new_blog_path %></p>
</div>

